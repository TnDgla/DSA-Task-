class Solution {
    public int buyChoco(int[] prices, int money) {
        // Arrays.sort(prices);
        // int sum=prices[0]+prices[1];
        // if(sum<=money){
        //     return money-sum;
        // }else{
        //     return money;
        // }
        
        int a=Integer_MIN_VALUE;
        //prices[0];
        for(int i=0;i<prices.length;i++){
           a=Integer.MIN_VALUE;
           
        }
        int min1=Integer.MIN_VALUE();
        System.out.print(min1);
    }
}
